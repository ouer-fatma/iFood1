{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ utilisateur.id }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ utilisateur.email }}</td>
        </tr>
        <tr>
            <th>Roles</th>
            <td>{{ utilisateur.roles ? utilisateur.roles|json_encode : '' }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ utilisateur.password }}</td>
        </tr>
        </tbody>
    </table>
    <div style="text-align: center;">
    <a class="btn btn-outline-primary mt-2 shadow" href="{{ path('membre') }}">Page membre</a><br><br>
    <a class="btn btn-outline-warning mt-2 shadow" href="{{ path('app_utilisateur_edit', {'id': utilisateur.id}) }}">Modifier</a>

    {{ include('utilisateur/_delete_form.html.twig') }}
    </div>
{% endblock %}