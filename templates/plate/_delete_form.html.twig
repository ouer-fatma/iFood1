<form method="post" action="{{ path('app_plate_delete', {'id': plate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plate.id) }}">
    <button class="btn">Delete</button>
</form>
