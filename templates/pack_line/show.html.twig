{% extends 'base.html.twig' %}

{% block title %}PackLine{% endblock %}

{% block body %}
    <h1>PackLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pack_line.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ pack_line.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pack_line_index') }}">back to list</a>

    <a href="{{ path('app_pack_line_edit', {'id': pack_line.id}) }}">edit</a>

    {{ include('pack_line/_delete_form.html.twig') }}
{% endblock %}
